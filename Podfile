# 指定平台和最低支持的 iOS 版本
platform :ios, '17.0'

# 使用源仓库
source 'https://github.com/CocoaPods/Specs.git'

# 你的项目的 target 名称，通常与你的 Xcode 项目名称一致
target 'newSwiftDataTest' do
  # 关闭自动生成的用户框架搜索路径
  use_frameworks!
  
  # 添加 Alamofire 依赖
  pod 'Alamofire', '~> 5.6'
end
